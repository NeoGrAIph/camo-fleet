# Sample environment configuration for local development and overrides.
# Copy this file to .env and adjust values before running Docker Compose or deploying manifests locally.

# Runner (sidecar) settings for VNC/noVNC exposure
RUNNER_VNC_WS_BASE=ws://localhost
RUNNER_VNC_HTTP_BASE=http://localhost
RUNNER_VNC_DISPLAY_MIN=100
RUNNER_VNC_DISPLAY_MAX=199
RUNNER_VNC_PORT_MIN=5900
RUNNER_VNC_PORT_MAX=5999
RUNNER_VNC_WS_PORT_MIN=6900
RUNNER_VNC_WS_PORT_MAX=6999
RUNNER_VNC_RESOLUTION=1920x1080x24

# Worker API to runner sidecar binding
WORKER_RUNNER_BASE_URL=http://runner:8070

# Control-plane worker registry.
# name        — произвольный идентификатор, отображается в UI и логах.
# url         — адрес API воркера.
# supports_vnc — воркер умеет работать с VNC-сессиями (true/false).
# vnc_ws      — базовый WS URL для live-предпросмотра (обязателен при supports_vnc=true).
# vnc_http    — HTTP URL к noVNC iframe (опционально, если нужен проксированный доступ).
CONTROL_WORKERS=[{"name":"worker-headless","url":"http://worker-headless:8080","supports_vnc":false},{"name":"worker-vnc","url":"http://worker-vnc:8081","supports_vnc":true,"vnc_ws":"ws://runner-vnc:6900","vnc_http":"http://runner-vnc:6900/vnc.html"}]
