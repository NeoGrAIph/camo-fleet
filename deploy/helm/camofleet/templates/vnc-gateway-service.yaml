{{- if .Values.vncGateway.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "camofleet.vncGateway.fullname" . }}
  labels:
    app: {{ include "camofleet.vncGateway.fullname" . }}
{{ include "camofleet.labels" . | indent 4 }}
spec:
  type: {{ .Values.vncGateway.service.type }}
  selector:
    app: {{ include "camofleet.vncGateway.fullname" . }}
  ports:
    - port: {{ .Values.vncGateway.service.port }}
      targetPort: http
      name: http
{{- end }}
